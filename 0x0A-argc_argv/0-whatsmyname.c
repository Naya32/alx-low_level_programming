#include <stdio.h>

/*
 * @main Prints the name of the source file followed by a new line.
 * @return 0 indicating successful execution.
 */

int main(void)
{
printf("%s\n", __FILE__);
return (0);
}

